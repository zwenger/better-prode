/**
 * TanStack Start entry point.
 * Exporting undefined is the correct default — this file tells TanStack Start
 * that no custom start configuration is needed.
 *
 * Required by the #tanstack-start-entry package import map so that rolldown
 * can resolve this specifier during SSR dep optimization with @cloudflare/vite-plugin.
 */
export const startInstance = undefined;
