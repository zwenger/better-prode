#!/usr/bin/env node
/**
 * Seeds the database with development fixtures.
 *
 * In PR 0 this is a stub. Full fixture data (WC2026 tournament, teams, matches,
 * users, predictions) is added in PR 6 (task 6.12 — db/seeds/wc2026.sql).
 *
 * Usage: npm run db:seed
 */
console.log(
  "db:seed — stub. Full seed data added in PR 6 (db/seeds/wc2026.sql)."
);
process.exit(0);
